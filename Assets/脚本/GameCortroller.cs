using Cinemachine;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameCortroller : MonoBehaviour
{
    public static bool isGameAlive = true;  //新建一个布尔值控制
    //public static CameraState camShake; //教程里有，但我跳了几节，这一次的那一节没用到这句
}
